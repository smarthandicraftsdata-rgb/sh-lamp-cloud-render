# RF6.0 Render package

The RF6.0 architectural separation is enforced primarily by ESP firmware and iOS routing/state ownership.
This backend source is the validated R3.3 Render baseline packaged for the RF6.0 deployment.

The local Devices tab never uses this backend as a control route.
Remote Cloud control uses this backend only when Remote Access is enabled on the physical lamp.
